# Core dependencies for the production pipeline (`src/`), DVC, and MLflow.
# Notebook-specific dependencies are listed separately in `notebooks/requirements.txt` (e.g. marimo, scipy, and plotly).

tensorflow[and-cuda]==2.21.0
tf-keras>=2.20,<2.22
transformers>=4.45,<5
polars==1.42.1
numpy==2.3.5
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
pyyaml==6.0.3
tqdm>=4.66.0
pyarrow>=25.0.0

# Data versioning and experiment tracking
dvc>=3.30.0
mlflow>=2.10.0
dagshub>=0.7.2
